const { get, post, put, delete: del } = require('axios').default

module.exports = describe('/recipe', () => {
    describe('GET', () => {
        it.skip("should list the current user's recipes", done => {
            done()
        })
    })
})
