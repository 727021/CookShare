const { get, post, put, delete: del } = require('axios').default

module.exports = describe('/auth', () => {
    describe('POST', () => {
        it.skip('should return username', done => {
            done()
        })
    })
})
