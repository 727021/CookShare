const { get, post, put, delete: del } = require('axios').default

module.exports = describe('/cookbook', () => {
    describe('GET', () => {
        it.skip("should list the current user's cookbooks", done => {
            done()
        })
    })
})
