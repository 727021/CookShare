const { get, post, put, delete: del } = require('axios').default

module.exports = describe('/user', () => {
    describe('GET', () => {
        it.skip('should return nothing', done => {
            done()
        })
    })
})
